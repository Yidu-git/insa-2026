### Bullet notes: 
- ping is used to check if IP is running
	- It returns the IP
- Scripts are files containing executable code
- Shell is the text based interface that intemperate commands
- `apt` = **Advanced Package Tool**
- `top` : Task manager
- `htop` : CLI task manager
	- exit : F9
# To research
- Analyze : 
```shell
 cat challenge1.txt | grep -oE 'FLAG{[a-zA-Z0-9_]+}' | sort -u | wc -l
```
